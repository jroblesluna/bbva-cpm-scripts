# AlwaysPrint Cloud Management - Backend Dependencies
# Python 3.12

# === CORE FRAMEWORK ===
fastapi>=0.115.0
websockets
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12

# === DATABASE ===
sqlalchemy>=2.0.36
alembic>=1.14.0
psycopg2-binary>=2.9.9  # PostgreSQL driver

# === AUTHENTICATION & SECURITY ===
python-jose[cryptography]>=3.3.0  # JWT
passlib[bcrypt]>=1.7.4  # Password hashing
bcrypt==4.1.3  # Versión específica para compatibilidad con passlib 1.7.4
pyjwt>=2.9.0

# === VALIDATION ===
pydantic>=2.9.0
pydantic-settings>=2.5.0
email-validator>=2.2.0

# === SYSTEM MONITORING ===
psutil>=6.1.0  # Métricas del sistema operativo (CPU, RAM, disco, swap)
docker>=7.1.0  # Docker SDK para recolectar stats de contenedores
apscheduler>=3.10.4  # Programación de tareas periódicas (recolección cada 6h)
tzlocal>=5.0  # Requerido por APScheduler para detección de timezone local

# === LOGGING ESTRUCTURADO ===
structlog>=24.1.0

# === UTILITIES ===
python-dotenv>=1.0.1
python-dateutil>=2.9.0

# === CACHE (OPTIONAL) ===
redis>=5.0.0

# === LOGGING ESTRUCTURADO ===
structlog>=24.1.0

# === HTTP CLIENT ===
httpx>=0.27.0  # Para providers LLM (OpenAI, Anthropic)

# === AWS ===
boto3>=1.35.0

# === DEVELOPMENT (OPTIONAL) ===
# pytest>=8.0.0
# pytest-asyncio>=0.23.0
# pytest-cov>=4.1.0
# httpx>=0.27.0  # For testing FastAPI
# black>=24.0.0  # Code formatter
# flake8>=7.0.0  # Linter
# mypy>=1.8.0  # Type checker
