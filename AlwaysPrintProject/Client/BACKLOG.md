# Backlog — AlwaysPrint Client

Tareas pendientes de baja prioridad que se abordarán en iteraciones futuras.

## Pendiente

- [ ] **logo_offline_dev.ico**: Crear variante del icono offline para entorno DEV. Actualmente `logo_offline.ico` es el mismo en ambos entornos (dev/prod), mientras que `logo.ico` y `logo.png` tienen variantes condicionales (`logo-dev.ico`, `logo-dev.png`). Agregar lógica condicional `Environment=dev` en el csproj igual que los otros logos.
